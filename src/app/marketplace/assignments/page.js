'use client';
import CategoryPage from '@/components/marketplace/CategoryPage';

export default function AssignmentsCategoryPage() {
  return (
    <CategoryPage 
      categoryName="Assignments"
      categoryEmoji="📄"
      categoryDesc="Find solved course assignments, lab records, experiment sheets, and project documents."
    />
  );
}
