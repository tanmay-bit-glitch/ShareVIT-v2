'use client';
import CategoryPage from '@/components/marketplace/CategoryPage';

export default function NotesCategoryPage() {
  return (
    <CategoryPage 
      categoryName="Notes"
      categoryEmoji="📝"
      categoryDesc="Browse and download class notes, lecture summaries, and handwritten slides shared by peers."
    />
  );
}
