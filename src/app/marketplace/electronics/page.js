'use client';
import CategoryPage from '@/components/marketplace/CategoryPage';

export default function ElectronicsCategoryPage() {
  return (
    <CategoryPage 
      categoryName="Electronics"
      categoryEmoji="💻"
      categoryDesc="Acquire or list scientific calculators, lab hardware components, development boards, and devices."
    />
  );
}
