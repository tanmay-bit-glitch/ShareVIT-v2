'use client';
import CategoryPage from '@/components/marketplace/CategoryPage';

export default function BooksCategoryPage() {
  return (
    <CategoryPage 
      categoryName="Books"
      categoryEmoji="📚"
      categoryDesc="Buy, rent, borrow, or donate core textbooks, reference guides, and entrance prep books."
    />
  );
}
