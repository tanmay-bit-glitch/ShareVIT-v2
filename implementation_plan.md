"# Auto-Fetch Attendance from VIERP\n\nWe want to automatically import attendance data from `vierp.in` into the ShareVIT Attendance Tracker.\n\n## Technical Analysis of vierp.in\nI have deeply analyzed the source code of `vierp.in` and discovered two major technical roadblocks:\n1. **It's a Single Page Application (SPA):** The website's HTML is completely blank when loaded. It relies 100% on JavaScript to render the login page and dashboard. This means fast, lightweight backend scraping (like `cheerio`) will simply pull a blank page.\n2. **Google reCAPTCHA v3:** They have enterprise-grade bot protection (`<script src=\"https://www.google.com/recaptcha/api.js\">`) running silently on the page.\n\n> [!WARNING]\n> Because of the SPA architecture and reCAPTCHA, we **cannot** build a silent backend scraper in Next.js. Any automated bot that tries to log in with a username and password will be instantly blocked by Google's anti-bot systems. \n\nTo give you the automated experience you want, we have two realistic options.\n\n---\n\n## Proposed Options\n\n### Option 1: AI Screenshot Scanner (Highly Recommended)\nInstead of fighting bot protection, the student logs into `vierp.in`, takes a screenshot of their attendance table, and uploads it to ShareVIT.\n* We use the Gemini Vision API to instantly \"read\" the image.\n* It automatically extracts every subject name, total classes, and attended classes.\n* It auto-fills the entire Attendance Tracker in 2 seconds.\n* **Pros:** 100% reliable, zero security risk, no passwords needed.\n\n### Option 2: ShareVIT Chrome Extension\nWe build a dedicated browser extension for ShareVIT. \n* When a student logs into `vierp.in` normally on their own laptop, they click the extension button.\n* The extension grabs the attendance data directly from the active webpage and securely sends it to their ShareVIT account.\n* **Pros:** Seamless \"one-click\" sync. \n* **Cons:** Requires students to install a separate extension from the Chrome Web Store. Takes much longer to develop.\n\n---\n\n> [
<truncated 308 bytes>